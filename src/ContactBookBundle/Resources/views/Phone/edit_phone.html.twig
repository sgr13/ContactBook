{% extends 'base.html.twig' %}

{% block body %}
    <h1>Zmień nr telefonu</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Dodaj" />
    {{ form_end(form) }}

    <a href="{{ path('userShowAll') }}"><button>Powrót</button></a>

{% endblock %}